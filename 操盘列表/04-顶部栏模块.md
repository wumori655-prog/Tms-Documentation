好，整份完整文件在这里。Ctrl + A 全选复制 → GitHub 网页编辑 → 整份贴上覆盖即可。
⚠️ 改动标示规则：

旧内容（要删除的）→ 用 ~~删除线~~ 包起来（保留让你看到改了什么）
新内容 → 直接写
你最后可以保留删除线版本 让团队 review 看清楚改了什么，或者最后一次 commit 把删除线整段拿掉


从下面 --- 开始复制：

markdown# 第四章 顶部导航栏与顶部栏模块

## 交互原型

> **体验顶部栏交互**：观察顶部栏的统计卡片，点击「一键锁盘」按钮查看异常清单弹窗。

## 4.1 顶部导航栏（M00）

顶部导航栏固定在页面最顶部，高度48像素，包含系统Logo、一级菜单、消息通知和用户信息。

### 4.1.1 导航栏结构
┌─────────────────────────────────────────────────────────────────────────────┐
│ 🐻 金刚管理系统 │ Dashboard▼│商户管理▼│数据源管理▼│配置管理▼│...│操盘管理▼│ 🔔 张三 │
└─────────────────────────────────────────────────────────────────────────────┘

### 4.1.2 一级菜单列表

| 菜单项       | 图标 | 二级菜单                               | 说明         |
| ------------ | ---- | -------------------------------------- | ------------ |
| Dashboard    | 📊   | 主控台                                 | 系统仪表盘   |
| 商户管理     | 🏛️   | 商户列表                               | 商户信息管理 |
| 数据源管理   | 📡   | 数据源列表                             | 数据源配置   |
| 配置管理     | ⚙️   | 盘口大类、全局配置、运动类型、系统币种 | 系统配置     |
| 用户管理     | 👥   | 用户列表                               | 用户账号管理 |
| 数据管理     | 📋   | 投注记录、交易明细                     | 数据查询     |
| 体育管理     | 🏆   | 赛事组别、赔率类型、串关类型           | 体育配置     |
| **操盘管理** | 🎯   | **操盘列表**、操盘日志、联赛管理       | **当前模块** |
| 结算管理     | 💰   | 待结算、异常赛事、结算记录             | 赛事结算     |
| 翻译管理     | 🌐   | 语言翻译                               | 多语言管理   |
| 权限管理     | 🔐   | 角色权限、菜单权限、后台用户           | 权限配置     |
| 系统应用     | 📦   | 附件管理                               | 系统工具     |

### 4.1.3 菜单交互规则

| 交互             | 行为                     |
| ---------------- | ------------------------ |
| 鼠标悬停菜单项   | 显示下拉二级菜单         |
| 点击二级菜单     | 跳转到对应页面           |
| 当前页面菜单     | 一级菜单高亮显示蓝色背景 |
| 当前页面二级菜单 | 二级菜单文字高亮蓝色     |
| 鼠标移出         | 300ms延迟后收起下拉菜单  |

### 4.1.4 用户区域

| 元素         | 说明                                |
| ------------ | ----------------------------------- |
| 消息通知图标 | 显示未读消息数量角标，超过99显示99+ |
| 用户头像     | 圆形头像，显示姓氏首字              |
| 用户名称     | 显示姓名和角色标签                  |

点击用户区域展开下拉菜单，包含个人设置和退出登录选项。

---

## 4.2 顶部栏（M02）

顶部栏位于主内容区顶部，显示当前页面的全局统计信息和紧急操作按钮。

### 4.2.1 顶部栏结构
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│  操盘列表  │  ●IM 正常  │  待上架 156  滚球中 23  总赛事 320  预估盈亏 ¥125K  延期 2  │  服务器时间 14:32:45  │  🔒 一键锁盘  🔓 一键解锁  │
└─────────────────────────────────────────────────────────────────────────────────────────────┘

### 4.2.1.1 数据源状态指示器

> **v1.3 新增**：数据源状态从侧边栏移至顶部栏

| 元素       | 说明                                        | 默认值 |
| ---------- | ------------------------------------------- | ------ |
| 状态圆点   | 6px圆形，正常绿色带发光；延迟橙色；异常红色 | 正常   |
| 数据源名称 | 显示"IM"                                    | IM     |
| 状态文字   | 正常/延迟/异常                              | 正常   |

**状态判定规则**：

| 状态 | 圆点 | 颜色 | 条件                         |
| ---- | ---- | ---- | ---------------------------- |
| 正常 | 🟢   | 绿色 | 在预期刷新周期内收到更新     |
| 延迟 | 🟡   | 橙色 | 超过警告阈值但未达异常阈值   |
| 异常 | 🔴   | 红色 | 超过异常阈值或无响应         |

详细阈值定义见[第3章侧边栏模块](./03-侧边栏模块.md#_3-2-数据源状态区)。

### 4.2.2 统计卡片

统计卡片显示关键指标，**默认按用户可见范围统计**，不受筛选条件影响。主管和风控可通过「全局视图」开关切换为全局统计口径。

| 统计项   | 默认口径（普通操盘手） | 主管/风控口径 | 显示规则                   | 点击行为                       |
| -------- | ---------------------- | ------------- | -------------------------- | ------------------------------ |
| 待上架   | 可见范围内待上架赛事   | 全局统计      | 白色数字                   | 重置筛选条件，切换到待上架筛选 |
| 滚球中   | 可见范围内滚球赛事     | 全局统计      | 红色数字，数量>0时脉动动画 | 重置筛选条件，切换到滚球Tab    |
| 总赛事   | 可见范围内赛事总数     | 全局统计      | 白色数字                   | 无                             |
| 预估盈亏 | 可见范围内预估盈亏     | 全局统计      | 正数绿色，负数红色         | 无                             |
| 延期     | 可见范围内延期赛事     | 全局统计      | 橙色数字                   | 重置筛选条件，筛选延期赛事     |

> **统计口径说明**：统计数字默认按用户数据可见范围计算。主管和风控可通过顶部栏右侧的「全局视图」开关切换为全局统计口径。Tab页签数字在当前可见范围基础上，受日期、联赛等筛选条件影响。

#### 数据更新机制

统计数据通过WebSocket实时推送更新，推送频率与赛事状态变化同步。详见[第13章「实时数据更新机制」](./13-实时数据更新.md)。

### 4.2.3 服务器时间

服务器时间用于操盘手校准本地时间，避免因时间偏差导致的操作误判（如误判即将开赛赛事）。

| 显示项     | 格式                 | 更新频率 |
| ---------- | -------------------- | -------- |
| 服务器时间 | HH:mm:ss（北京时间） | 每秒更新 |

#### 时间差异告警

| 本地与服务器时间差 | 显示状态     | 说明                                   |
| ------------------ | ------------ | -------------------------------------- |
| ≤3秒               | 正常显示     | 无需处理                               |
| 3-10秒             | 黄色警告图标 | 提示「时间偏差较大，请检查本地时间」   |
| >10秒              | 红色警告图标 | 提示「时间严重偏差，影响操盘判断」 |

### 4.2.4 紧急操作按钮

#### 一键锁盘

> **v2.0 重大改造**（2026-05-22）：~~一键锁盘用于紧急情况下快速锁定所有滚球赛事，防止异常投注涌入。~~ 改造为「**异常清单 + 勾选式**」操作，避免无差别全锁导致的收益损失。

一键锁盘用于紧急情况下快速锁定**有告警的异常赛事**盘口，防止异常投注涌入。

| 属性     | 说明                                           |
| -------- | ---------------------------------------------- |
| 按钮样式 | 红色边框，白色文字                             |
| 按钮文字 | 🔒 一键锁盘                                     |
| 权限要求 | 仅操盘主管、风控主管角色可见                   |
| 快捷键   | Ctrl + Shift + L（需焦点在操盘列表页）         |
| 行为     | 点击 → 弹出「异常赛事清单」弹窗（不再直接全锁）|

##### 弹窗：异常赛事清单

**弹窗显示规则**：

| 规则 | 说明 |
|------|------|
| 默认显示 | 仅显示「有告警」的赛事（货量大 / 单边超限 / IM 异常 / 偏差大等）|
| 多告警显示 | 单一赛事的**所有告警标签并列显示**（不去重）|
| 自动刷新 | **每 5 秒自动刷新**（也提供手动刷新按钮）|
| 排序 | 按告警严重度排序（红 > 橙 > 黄），同色按时间排序 |
| 颗粒度 | 赛事级（不是联赛级）|
| 默认勾选 | **不自动勾选**，操盘手主动勾 |

**告警类型与视觉**：

| 告警 | 颜色 | 触发条件 | 来源 |
|------|------|---------|------|
| 货量大（单笔）| 🔴 红 | 单笔投注超阈值 | 风控管理定义 |
| 货量大（累计）| 🔴 红 | 单场累计超阈值 | 风控管理定义 |
| 单边超限 | 🟠 橙 | 满足风险敞口 6.5 节 A AND B 条件 | 风险敞口管理 |
| 数据源延迟 | 🔴 红 | IM 推送 > 5s | 操盘列表 16.5 |
| IM 异常 | 🟡 黄 | IM 暂停 / 维护 | 操盘列表 16.5 |
| 偏差大 | 🟠 橙 | IM vs 本地差异过大 | 操盘详情 11 |

##### 操作流程

| 步骤 | 用户操作 | 系统响应 |
|------|---------|---------|
| 1 | 点击「一键锁盘」按钮 | 弹出「异常赛事清单」弹窗 |
| 2 | 查看清单并勾选要锁定的赛事 | 底部显示「已选 X / Y 场」|
| 3 | 点击「锁定选中」按钮 | 弹出二次确认弹窗 |
| 4 | 填写锁定原因（选填）→ 确认锁定 | 批量执行赛事级锁定 |
| 5 | 执行完成 | Toast 显示「已锁定 X 场」+ 记录操作日志 |

##### 异常清单弹窗格式
┌─────────────────────────────────────────┐
│ 🔒 紧急锁盘 - 异常赛事清单    [刷新] [✕]│
├─────────────────────────────────────────┤
│ 系统识别 X 场异常赛事，请勾选要锁定的：  │
│                                         │
│ ☐ 全选                                  │
│                                         │
│ ☐ 赛事 ID  对阵                         │
│    [告警标签 1] [告警标签 2]            │
│    阶段  联赛                           │
│                                         │
│ ... (滚动)                              │
│                                         │
│ ⏱ 自动刷新中（每 5 秒）  已选 0 / X 场 │
│                                         │
│        [取消]      [🔒 锁定选中]        │
└─────────────────────────────────────────┘

##### 二次确认弹窗
┌─────────────────────────────────────────┐
│ ⚠️ 确认锁定 X 场赛事                    │
├─────────────────────────────────────────┤
│ 锁定后：                                │
│ • 该赛事所有盘口将停止接受投注          │
│ • 数据源状态变更不会自动解锁            │
│ • 必须由主管或风控手动解锁              │
│                                         │
│ 锁定原因（选填）：                      │
│ [_____________________________]         │
│                                         │
│        [取消]      [🔒 确认锁定]        │
└─────────────────────────────────────────┘

##### 锁定后行为

| 范围 | 行为 |
|------|------|
| 锁定层级 | 赛事级（该赛事所有盘口锁定）|
| 客户端 | 玩家可见，显示「暂停投注」灰显 |
| 操盘端 | 列表显示「🔒 锁定中」标签 |
| 状态优先级 | 高于数据源暂停 / 联赛暂停，低于数据源关盘 |
| 日志记录 | 操作者 / 锁定时间 / 影响赛事 ID / 锁定原因 / 触发告警类型 |

##### ~~旧版锁定范围说明（v2.0 已废弃）~~

> ~~执行一键锁盘后，以下赛事的盘口状态变为「锁定」：~~
>
> | ~~条件~~                                             | ~~是否锁定~~ |
> | ------------------------------------------------ | -------- |
> | ~~上架状态=已上架 且 比赛进程=滚球~~                 | ~~是~~       |
> | ~~上架状态=已上架 且 比赛进程=即将（10分钟内开赛）~~ | ~~是~~       |
> | ~~上架状态=已上架 且 比赛进程=赛前~~                 | ~~否~~       |
> | ~~上架状态=待上架 或 已下架~~                        | ~~否~~       |
> | ~~盘口状态已为锁定或关盘~~                           | ~~跳过~~     |
>
> ~~**「即将」vs「紧急」区分**：~~
> - ~~**即将状态（比赛进程）**：≤30分钟，第9章定义的比赛进程分类标准~~
> - ~~**紧急阈值（一键锁盘）**：≤10分钟，一键锁盘、紧急告警、置顶规则使用的紧急判断阈值~~
>
> ~~v2.0 改造说明：旧版「无差别全锁滚球+10分钟内」会导致大量无告警赛事误锁、影响公司收益，已改为「异常清单 + 勾选式」精准锁定。~~

##### 解锁方式

**新设计：一键解锁** ⭐（v2.0 新增）

操盘列表顶部栏新增「🔓 一键解锁」按钮，详见 4.2.4 节下方。

> ~~**批量解锁说明**：一键锁盘执行后，**不支持批量解锁**。操盘手需在详情页逐场确认后解锁。这是刻意设计——批量锁定是紧急避险措施，但解锁必须逐场确认风险已消除，防止误恢复。~~
>
> **v2.0 设计变更**：旧版「不支持批量解锁」的限制已取消（业务需求驱动）。改为「弹窗 + 清单 + 勾选」对称设计，搭配「**必填解锁原因 + 二次确认**」做审计追责。详见下方 4.2.4 一键解锁章节。

#### 一键解锁

> **v2.0 新增**（2026-05-22）：与「一键锁盘」对称设计，用于批量解锁先前被锁定的赛事。

| 属性     | 说明                                           |
| -------- | ---------------------------------------------- |
| 按钮样式 | 绿色边框，白色文字                             |
| 按钮文字 | 🔓 一键解锁                                     |
| 显示条件 | 当前有「人工锁定」的赛事时显示，无锁定时灰显或隐藏 |
| 权限要求 | 仅操盘主管、风控主管角色可见                   |
| 行为     | 点击 → 弹出「已锁定赛事清单」弹窗              |

##### 弹窗：已锁定赛事清单

**弹窗显示规则**：

| 规则 | 说明 |
|------|------|
| 默认显示 | 所有「当前处于人工锁定状态」的赛事 |
| 显示信息 | 锁定时间 / 锁定人 / 锁定原因 / 联赛 / 当前阶段 |
| 自动刷新 | 每 5 秒自动刷新 |
| 排序 | 按锁定时间排序（最早锁的在最上）|
| 默认勾选 | 不自动勾选 |

##### 操作流程

| 步骤 | 用户操作 | 系统响应 |
|------|---------|---------|
| 1 | 点击「一键解锁」按钮 | 弹出「已锁定赛事清单」弹窗 |
| 2 | 查看清单并勾选要解锁的赛事 | 底部显示「已选 X / Y 场」|
| 3 | 点击「解锁选中」按钮 | 弹出二次确认弹窗 |
| 4 | **填写解锁原因（必填）** → 确认解锁 | 批量执行赛事级解锁 |
| 5 | 执行完成 | Toast 显示「已解锁 X 场」+ 记录操作日志 |

##### 已锁清单弹窗格式
┌─────────────────────────────────────────┐
│ 🔓 一键解锁 - 已锁定赛事清单  [刷新] [✕]│
├─────────────────────────────────────────┤
│ 当前共有 X 场赛事处于锁定状态：         │
│                                         │
│ ☐ 全选                                  │
│                                         │
│ ☐ 赛事 ID  对阵                         │
│    🔒 锁定时间 by 操作人                │
│    原因：[锁定原因]                     │
│    阶段  联赛                           │
│                                         │
│ ... (滚动)                              │
│                                         │
│ ⏱ 自动刷新中（每 5 秒）  已选 0 / X 场 │
│                                         │
│        [取消]      [🔓 解锁选中]        │
└─────────────────────────────────────────┘

##### 二次确认弹窗
┌─────────────────────────────────────────┐
│ ⚠️ 确认解锁 X 场赛事                    │
├─────────────────────────────────────────┤
│ 解锁后：                                │
│ • 这些赛事盘口将恢复接受投注            │
│ • 解锁前请确认风险已消除                │
│                                         │
│ 解锁原因（必填）：                      │
│ [_____________________________]         │
│                                         │
│        [取消]      [🔓 确认解锁]        │
└─────────────────────────────────────────┘

> **重要**：解锁原因为**必填**，用于审计追责，避免误解锁。

##### 解锁后行为

| 项目 | 行为 |
|------|------|
| 盘口状态 | 恢复到锁定前的状态（`pre_lock_status`）|
| 客户端 | 恢复正常显示 + 可投注 |
| 日志记录 | 操作者 / 解锁时间 / 影响赛事 / 解锁原因 / 锁定持续时间 |

---

## 修订记录

| 版本 | 日期       | 修订内容                                                                                                                                                                                                                   |
| ---- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v1.0 | 2026-01-15 | 初稿                                                                                                                                                                                                                       |
| v1.1 | 2026-01-20 | 1) 统计卡片改为五个（待上架、滚球中、总赛事、预估盈亏、延期），移除告警统计；2) 移除"与批量锁盘的区别"和"批量解锁"（列表页不提供盘口操作）；3) 解锁方式统一为详情页逐个解锁；4) 移除弹窗中的"当前总投注额"字段 |
| v1.2 | 2026-01-30 | 新增4.2.1.1全局搜索框（从侧边栏移入）、4.2.1.2数据源状态指示器（从侧边栏移入） |
| v1.3 | 2026-01-30 | 移除全局搜索框（回滚至侧边栏），仅保留数据源状态指示器 |
| **v2.0** | **2026-05-22** | **一键锁盘重大改造**：1) 旧版「无差别全锁滚球+10分钟内」改为「异常清单 + 勾选式」精准锁定；2) 新增「一键解锁」对称设计（弹窗+清单+勾选），取消「不支持批量解锁」的限制；3) 解锁原因必填（审计追责）；4) 异常清单弹窗自动刷新（每 5 秒）；5) 颗粒度=赛事级；6) 不自动勾选 |

✅ 复制贴上完成后

全选这份内容 → 贴到 GitHub 网页编辑
拉到底 → Commit changes
等 30-60 秒 → 重整网站确认


💡 关于「删除线」的处理
我用了 ~~删除线~~ 标记旧内容，这样有 2 个好处：
好处说明Review 方便团队 review 时一眼看到改了什么可回滚万一新设计有问题，能快速恢复旧版
